<h1>Admin::Users#show</h1>
<p>Find me in app/views/admin/users/show.html.erb</p>

<h1><%= @user.name %>の情報</h1>
<p><strong>Name:</strong> <%= @user.name %></p>
  <p><strong>Email:</strong> <%= @user.email %></p>
<%= link_to 'Delete Account', admin_user_path(@user), method: :delete, data: { confirm: 'Are you sure you want to delete your account?' }, class: 'btn btn-danger' %>

<h1><%= @user.name %>の投稿一覧</h1>

<% @posts.each do |post| %>
  <div>
    <h3><%= link_to post.text, admin_post_path(post) %></h3>
<% end %>

<h2><%= @user.name %>が送信した過去のコメント一覧</h2>
<% @user.comments.where(user_id: @user.id).each do |comment| %> <!-- ユーザーIDでフィルタリング -->
  <div>
    <%= link_to comment.comment, admin_comment_path(comment) %> <!-- コメントをクリックすると詳細ページに遷移 -->
    <p><%= comment.created_at.strftime('%Y/%m/%d') %></p>
  </div>
<% end %> <!-- このendで繰り返しを終了 -->
